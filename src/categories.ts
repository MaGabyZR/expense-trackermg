//Have all categories in a single place and export them to reuse them in multiple components. Add as const to make it a read only array.
const categories = ["Groceries", "Utilities", "Entertainment"] as const;

export default categories;
